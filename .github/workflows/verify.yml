
name: "Verify"
on: "push"

jobs:
    checking:
        runs-on: ubuntu-latest
        defaults:
            run:
               shell: bash
        steps:
        -   name: "Version"
            run: penguin --version
        -   name: "Dependency"
            run: penguin --dependency
        -   name: "Environment"
            run: penguin --environment
    
